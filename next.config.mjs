/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['enka.network', 'raw.githubusercontent.com', 'api.yatta.top'], // เพิ่มโดเมนที่คุณต้องการ
    },
};

export default nextConfig;
